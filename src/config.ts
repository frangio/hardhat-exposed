export interface ExposedUserConfig {
  prefix?: string;
  exclude?: string[];
  include?: string[];
  outDir?: string;
}

export interface ExposedConfig {
  prefix?: string;
  exclude: string[];
  include: string[];
  outDir: string;
}
